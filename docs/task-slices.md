# KisMac3 — Task Slices & Acceptance Matrix

Owner: `kismac-program-lead`. This turns the `docs/modernization-plan.md` milestone
roadmap into small, independently-shippable task slices, ordered by dependency, each
with a single implementation owner, an independent reviewer, a validation owner
(the PR gate from `docs/subagent-orchestration.md`), explicit acceptance criteria,
and the upstream parity decisions from `docs/original-feature-parity.md`.

**Sizing:** each slice is meant to be one PR — roughly 1–3 days of focused work.
Larger milestones are split. Slices marked **[blocked]** must not start until their
dependencies land.

**Reading the table:** `Impl` = implementation agent, `Rev` = reviewer, `Val` =
validation agent (all defined in `.claude/agents/`). `Deps` = slice IDs that must
land first.

---

## Wave 0 — Baseline (do first, mostly sequential)

These get the project compiling and observable on current Xcode before any feature
work. They unblock everything else.

> **Wave 0 build invocation (env note from S0.1):** this machine has no `DMP42GVPJ3`
> dev signing cert (iCloud-hosted repo), so default `xcodebuild` fails at provisioning.
> For all Wave 0 builds, disable signing —
> `CODE_SIGNING_ALLOWED=NO CODE_SIGNING_REQUIRED=NO CODE_SIGN_IDENTITY=""` — or set a
> local team. Signing itself is owned by **S8.x** (tracked as ENV-1 in `docs/build-triage.md`).
> Toolchain observed: Xcode 26.5, macOS 27.0, arm64, SDK 26.5, deployment target 13.0.

### S0.1 — Compile baseline: inventory & triage build failures — ✅ DONE (`b34b732`)
- **Milestone:** 4 · **Impl:** legacy-objc-porting-agent · **Rev:** objc-appkit-reviewer · **Val:** xcode-build-validator · **Deps:** —
- **Scope:** Attempt a clean build of `KisMac2.xcworkspace` on current Xcode (after `git submodule update --init --recursive`). Catalogue every error/warning into a triage list; do **not** fix yet. Confirm submodules (`BIGeneric`, `BIGL`, `polarssl`) build.
- **Acceptance:** A committed `docs/build-triage.md` listing each failure with file:line and a proposed owner slice. `xcode-build-validator` confirms the error list is reproducible.
- **Outcome:** `docs/build-triage.md` committed. BIGL/BIGeneric subprojects build (123 warnings, all OpenGL/Carbon deprecation → S5.x). The KisMac2 target aborts at its **first** phase (`Update vendors.db` run-script) due to a Python `IndentationError` introduced by the pre-existing WIP commit `9372017` — so the S0.3/S0.4 compile-breakers are real-in-source but **not yet build-confirmed** (masked behind the script failure). This spawned **S0.6**. **S0.1 must be RE-RUN after S0.6 + S0.2 land** to capture the build-confirmed compiler errors.

### S0.6 — Fix the `OUItoVendorDB.py` "Update vendors.db" build-script phase
- **Milestone:** 4 · **Impl:** legacy-objc-porting-agent · **Rev:** objc-appkit-reviewer · **Val:** xcode-build-validator · **Deps:** S0.1
- **Scope:** Repair the Python `IndentationError` at `Resources/Generic/OUItoVendorDB.py:49` (de-indent the `if filesize:` progress block to sit inside the `while` download loop). This is the **first** build phase of the KisMac2 target, so it must pass before any source compiles — the true first build-unblocker. (Root cause: the WIP commit `9372017` added an over-indented line; verify the rest of that WIP Python change is sound while here.)
- **Acceptance:** `python3 -m py_compile Resources/Generic/OUItoVendorDB.py` clean; the `Update vendors.db` phase succeeds (or is confirmed to fetch/skip correctly offline); the KisMac2 build proceeds past it to the compile phase. `xcode-build-validator` confirms the build now reaches source compilation.

### S0.2 — Remove dead bundled Growl framework
- **Milestone:** 4 · **Impl:** legacy-objc-porting-agent · **Rev:** release-reviewer · **Val:** xcode-build-validator · **Deps:** S0.1
- **Scope:** Strip `Resources/Growl.framework` from the Frameworks + CopyFiles build phases and from disk; confirm no code references remain (Growl removal already committed in `9372017`).
- **Acceptance:** Builds with no Growl references; `otool -L` on the product shows no Growl; app launches. Parity: notifications row stays 🔧.

### S0.3 — Fix compile-breaking removed APIs — ✅ DONE (done together with S0.5)
- **Milestone:** 4 · **Impl:** legacy-objc-porting-agent · **Rev:** objc-appkit-reviewer · **Val:** xcode-build-validator · **Deps:** S0.1
- **Scope:** Replace `NSDate descriptionWithCalendarFormat:`/`NSCalendarDate` (export paths in `WaveStorageController.m:327,477,513`), and quarantine the unlinkable `WirelessCryptMD5`/`WirelessEncrypt` "Apple" WEP wordlist verbs (`WaveNetWEPWordlist.m`) per the 🗑 decision. Replace `NSBeginAlertSheet`/`NSRunCriticalAlertPanel` in touched files with `NSAlert`.
- **Acceptance:** Project compiles past these errors; the three removed WEP verbs are disabled with a clear "unsupported (removed Apple API)" path, not a crash. `xcode-build-validator` reports a successful Debug build.
- **Outcome:** Re-characterized per the post-S0.6 build-triage finding (these were deprecation **warnings**, not hard errors — build was already green; the bar moved to "migrate without regressing"). The `WirelessCryptMD5` "Apple WEP wordlist" item was already retired by **S2.3** (moved to CommonCrypto) so it was NOT redone here. **descriptionWithCalendarFormat:** all 6 active call sites migrated off the removed `NSCalendarDate` category — added a `strftime(3)`-based helper `+[WaveHelper stringFromDate:strftimeFormat:timeZone:]` (the legacy format strings are classic strftime patterns, e.g. `%Y-%m-%d`/`%H:%M:%S`, NOT Unicode `NSDateFormatter` patterns, so strftime is the faithful, behaviour-preserving replacement; GMT honoured via `secondsFromGMTForDate:`+`gmtime_r`, local via `localtime_r`). Migrated `WaveStorageController.m` (4), `WavePcapDump.m` (1, user dump-path strftime), `WaveScanner.mm` (1, user dump-path strftime). The 2 commented-out `WaveNet.mm`/`WaveClient.m` sites were left inert. **Alert family (~40 sites across 16 files):** `NSBeginAlertSheet`/`NSBeginCriticalAlertSheet`/`NSRunAlertPanel`/`NSRunCriticalAlertPanel`/`NSRunInformationalAlertPanel` → modern `NSAlert`. Added consistent `+[WaveHelper showInformationalAlertWithTitle:message:]` / `showCriticalAlertWithTitle:message:` / `showModalAlertWithTitle:message:style:buttons:]` helpers (main-thread-safe, mirror the S2.4 `KMKismetShowError` pattern; format-arg sites preserved via `stringWithFormat:`). The **3 sheets with real didEnd callbacks** (`-[ScanControllerPrivate showWantToSaveDialog:]`, `-[BridgeController showWantToSaveDialog:]`, `-[ScanControllerMenus reallyWantToDelete…]`) were migrated to `-beginSheetModalForWindow:completionHandler:` — and this **fixed a latent bug**: the didEnd handlers already switch on the modern `NSAlertFirst/Second/ThirdButtonReturn` codes (a prior slice migrated them), which did NOT match the legacy `NSBeginAlertSheet` default/alternate/other (1/0/-1) return codes — `NSAlert` returns the modern codes, so Save/Delete/Cancel routing now matches. The value-returning `+[WaveHelper showCouldNotInstaniciateDialog:]` preserves its legacy `==1` Retry contract. **NSAlertDefault/Alternate/OtherReturn** (13) → `NSAlertFirst/Second/ThirdButtonReturn`. `IOMasterPort`→`IOMainPort` (3, deployment target 13.0 ≥ macOS 12). Build stayed **green** throughout; all migrated-symbol warnings = **0**. (Counts + remaining floor: see S0.5.)

### S0.4 — Quarantine dead i386/Carbon code — ✅ DONE
- **Milestone:** 4 · **Impl:** legacy-objc-porting-agent · **Rev:** objc-appkit-reviewer · **Val:** xcode-build-validator · **Deps:** S0.1
- **Scope:** Remove the `#ifdef __i386__` MIDI plugin (`WavePluginMidi.m`, 🗑) and the dead `HTTPStream`/`exportToServer:` stub + crash-report upload (🗑). Leave hooks documented.
- **Acceptance:** Builds clean; menu items for removed features are gone or disabled with a reason; no dead-endpoint network calls remain. `security-abuse-reviewer` confirms the crash-upload privacy path is gone.
- **Outcome:** Three dead 🗑 features removed, no replacements. **(1) MIDI plugin:** deleted `Sources/WavePlugins/WavePluginMidi.{h,m}` (the `#ifdef __i386__` Carbon QuickTime Music sonification — a no-op on x86_64/arm64), its `WaveScanner.mm` `@"MidiTrack"` plugin registration, and the wiring that existed ONLY to feed it — `-[ScanController trackClient:]` + the `InfoController` client-table `setDoubleAction:` that invoked it, and `-[ScanControllerMenus monitorSignal:]`/`-monitorAllNetworks:` (verified MIDI-only: they did nothing but set `WavePluginMidi setTrackString…`). Removed their two `NSMenuItem` outlets (`_monitorMenu`/`_monitorAllMenu`) + IBAction decls from `ScanController.h`, and the two Network-menu items ("Monitor Signal Strength" / "Monitor all signals") + trailing separator + the two outlet connections from `MainMenu.xib`. **(2) Server export:** deleted `Sources/Support/HTTPStream.{h,m}` (dead transport, never referenced beyond the import), the empty `-exportToServer:` stub + its decls + import, and the "  .kismac Server" Export-menu item (id 1159) from `MainMenu.xib`. **(3) Crash-report upload (PRIVACY):** deleted `Sources/WindowControllers/CrashReportController.{h,m}` entirely — it read the user's **Address Book "me" email** (`ABAddressBook`/`ABPerson`/`kABEmailProperty`) and POSTed it + crash logs to the long-dead `http://kismac-ng.org/crash.php` over plaintext HTTP — and removed its startup invocation in `-[ScanController applicationDidFinishLaunching:]` (the whole `SupressCrashReport` crash-log-collection block + its now-unused locals). Removed `AddressBook.framework` from the Frameworks build phase + project refs (it had NO other consumer in `Sources/`). **pbxproj:** 22 lines stripped (3 PBXBuildFile, 6 PBXFileReference, group children, Sources-phase entries, AddressBook.framework refs); `plutil -lint` OK, no orphan GUIDs. **0xA7 author byte** in `ScanControllerMenus.m` + `InfoController.mm` was mangled by Edit and **byte-restored** (git diff confirms the Author line is unchanged). **Verify:** FRESH clean Debug build **BUILD SUCCEEDED** (Xcode 26.5 / macOS 27 / arm64, signing disabled); app launches, alive 8s, terminated clean — **no startup crash-reporter window, no Contacts/Address Book prompt** in the launch log (startup goes straight to the capability/probe flow). `rg` confirms `HTTPStream`, `WavePluginMidi`, `ABAddressBook`/`AddressBook`, and `kismac-ng.org/crash.php` no longer appear in any compiled source (only in removal-note comments). **No regressions:** `KISMAC_CAP_SELFTEST` PASS, `KISMAC_SAFETY_SELFTEST` PASS, `KISMAC_ACTIVEGATE_SELFTEST` PASS, `KISMAC_CRYPTO_SELFTEST` ALL PASS, `KISMAC_PROTO_SELFTEST` 12/12, `KISMAC_PCAP_IMPORT_SELFTEST` 3 nets/7 clients PASS, `KISMAC_BLE_SELFTEST`/`KISMAC_USBINV_SELFTEST` (usb=10/hid=30/serial=3)/`KISMAC_LAN_SELFTEST`/`KISMAC_IFACE_SELFTEST` (33 ifaces) all PASS. **Intentionally kept:** the dead Help/Forum URLs `http://forum.kismac-ng.org/` + `http://trac.kismac-ng.org/...` (`ScanControllerMenus.m`, `RalinkJack.mm`) — those are the Cluster 5 dead-Help-link cleanup (S5.x), not the crash-upload endpoint, and are out of S0.4 scope.

### S0.5 — Clean Debug + Release build green — ✅ DONE (done together with S0.3)
- **Milestone:** 4 · **Impl:** legacy-objc-porting-agent · **Rev:** objc-appkit-reviewer · **Val:** xcode-build-validator · **Deps:** S0.6, S0.2, S0.3, S0.4
- **Scope:** Resolve remaining triage items to reach a warning-summarized clean build for both configurations; confirm Apple Silicon + Intel (or document which is unavailable).
- **Acceptance:** `xcode-build-validator`: clean Debug **and** Release build, product is the expected architecture, app launches outside Xcode (unsigned dev run). This is the **compile-baseline gate** for all later waves.
- **Outcome:** Done together with S0.3 (same files). **Warning burn-down (deduplicated unique warnings, fresh clean Debug, Xcode 26.5 / macOS 27 / arm64):** whole build **449 → 248** (−201); KisMac2 `Sources/` **315 → 115** (−200); `Sources/` `-Wdeprecated-declarations` **248 → 54** (−194). Mechanical token migrations (all behaviour-identical, byte-safe): `NSOnState`→`NSControlStateValueOn` (51), `NSOffState`→`…Off` (54), `NSFileHandlingPanelOKButton`→`NSModalResponseOK` (20), `NSCompositeSourceOver`/`Copy`→`NSCompositingOperation…` (5), `NSPNGFileType`/`NSJPEGFileType`→`NSBitmapImageFileType…` (2), `NSCenterTextAlignment`→`NSTextAlignmentCenter` (1), `NSWarningAlertStyle`→`NSAlertStyleWarning` (1), `NSLeftMouseUp(Mask)`/`NSLeftMouseDragged(Mask)`→`NSEventType…`/`NSEventMask…` (4), `NSTexturedBackgroundWindowMask` → for the borderless fullscreen window it became `NSWindowStyleMaskBorderless` (the textured mask is itself deprecated on macOS 11+ and meaningless for a chromeless window), `alertWithMessageText:defaultButton:…` → `NSAlert` init+`addButtonWithTitle:` (1), `stringByAddingPercentEscapesUsingEncoding:` → `stringByAddingPercentEncodingWithAllowedCharacters:[NSCharacterSet URLQueryAllowedCharacterSet]` (1, in `WaveHelper urlEncodeString:`). **Quality warnings:** all **6 `-Wmisleading-indentation`** sites fixed (a leading `\t` after an `if(){return;}` re-indented to 4 spaces in the USB/RT73/RT2570/RTL8187/Intersil drivers + WavePluginInjecting/InjectionProbe) — now **0**. **Header hygiene:** made the prefix-header-reliant legacy headers self-contained — `WavePacket.h`/`WaveDriverKismet.h`/`WaveDriverKismetDrone.h`/`WaveDriver.h` (+`<Foundation/Foundation.h>`/`<CoreFoundation/CoreFoundation.h>`), `WaveNet.h` (+`<Cocoa/Cocoa.h>` for its AppKit types), and the shared roots `KisMAC80211.h`/`KMCommon.h` (+`<CoreFoundation/CoreFoundation.h>` for `UInt8/16/32`). Additive, build stayed green. **Release build:** clean **Release** also **BUILD SUCCEEDED**; product is a **universal binary (x86_64 + arm64)**. Release forces a real `vendors.db` download via `OUItoVendorDB.py` — it hit **HTTP 418** (IEEE server anti-bot) and the **S0.6 fallback was buggy** (`elif not os.path.exists(...)` aborted the build when a vendor.db already existed). **Fixed** `OUItoVendorDB.py`: on download failure, keep the existing `vendor.db` (or write an empty plist if none) and exit 0 — Release now passes offline/blocked. **Launch UX (S3.3-flagged Location modal):** the default `GPSDevice` is `CoreLocation` and it IS started at launch via `applicationDidFinishLaunching:`→`updatePrefs:`→`initGPSControllerWithDevice:`, so CoreLocation fires at startup. The S1.2 `locationManagerDidChangeAuthorization:` already correctly does NOT alert on `notDetermined`, but `didFailWithError:` surfaced the remediation modal on `kCLErrorDenied` (Code 1) — which CoreLocation reports at launch **even while status is still notDetermined** (a launch nag). **Fixed** `GPSController.m didFailWithError:` to only surface remediation when the authorization status is **genuinely `denied`/`restricted`**, never for `notDetermined`. Verified at runtime: clean launch reaches the event loop, `status=0` notDetermined handled with **no blocking modal / no nag / no crash**, app terminated clean. **Architecture:** Apple Silicon (arm64) validated on this Mac15,8; the Release product is universal (x86_64+arm64) but **Intel hardware validation remains unavailable on this machine** (build-only for x86_64). **Signing** still disabled (ENV-1 → S8.x). **No regressions:** `KISMAC_CAP_SELFTEST` PASS, `KISMAC_SAFETY_SELFTEST` PASS, `KISMAC_ACTIVEGATE_SELFTEST` PASS, `KISMAC_PROTO_SELFTEST` 12/12, `KISMAC_CRYPTO_SELFTEST` ALL PASS, `KISMAC_PCAP_IMPORT_SELFTEST` 3 nets/7 clients, `KISMAC_BLE_SELFTEST` PASS, `KISMAC_USBINV_SELFTEST` usb=10/hid=30/serial=3, `KISMAC_LAN_SELFTEST` PASS, `KISMAC_IFACE_SELFTEST` 33 ifaces — all PASS.
  - **Remaining S5.x/S6.x/S1.x warning floor (the 54 `Sources/` deprecated-declarations left, by symbol):** `NSDrawer` **17** (→ split-view/sidebar, S5.x); `setAllowedFileTypes:` **15** + paired `imageFileTypes` **1** (→ UTType `allowedContentTypes` — deliberately **kept**: linking `UniformTypeIdentifiers.framework` is out of scope and the prior S2.2 slice found it failed to link; pcap/pcapng have no system UTType — S5.x/S8.x); `propertyListFromData:…`/`dataFromPropertyList:…` **8** (→ `propertyListWithData:`/`dataWithPropertyList:`, plist modernization, S6.x); `interfaceNames`/`interfaceWithName:` **8** (CoreWLAN, S1.x); `beginSheet:modalForWindow:modalDelegate:didEndSelector:contextInfo:` **5** (NSWindow sheet API, S5.x); `NSUserNotification`/`…Center` **3** (→ UserNotifications.framework, S5.x); `dateWithNaturalLanguageString:`/`dateWithString:` **3** + `CFPropertyListCreateFromXMLData` **1** (legacy parse, S5.x/S6.x); `unarchiveObjectWithData:`/`unarchiveObjectWithFile:` **2** (NSKeyedUnarchiver → secure `unarchivedObjectOfClasses:` — NOT straightforward: needs the whole `.kismac` object graph to adopt `NSSecureCoding`, **deferred to S6.x** per scope); Carbon Speech `GetVoiceDescription`/`GetIndVoice`/`CountVoices` **3** (→ AVSpeechSynthesizer, S5.x); `setMinSize:`/`setMaxSize:` **2** (S5.x). **Subprojects floor (unchanged, S5.x/S6.x):** BIGL/BIGeneric OpenGL/`NSOpenGL*` + Carbon Speech (~121) and GBStorage `archivedDataWithRootObject:`/`unarchiveObjectWithData:` (secure coding, S6.x) + 2 `-seg1addr` linker warnings (S8.x). **STATUS: ✅ DONE** (Debug + Release both green; the remaining floor is the documented S1.x/S5.x/S6.x/S8.x backlog, not Wave-0 work).

---

## Wave 1 — Capability foundation (the spine of the port)

### S1.1 — MacBook hardware capability probe — ✅ DONE (Apple Silicon) / partial validation (`2a96c99`)
- **Milestone:** 1 · **Impl:** macbook-capability-agent · **Rev:** macos-api-reviewer · **Val:** macbook-hardware-validation-agent · **Deps:** S0.5 (proceeded on the already-green **Debug** baseline post-S0.6; full S0.5 not required to compile)
- **Scope:** Implement the probe from Milestone 1: Apple Wi-Fi interface visibility, Location-permission state, CoreWLAN scan support, 2.4/5/6 GHz visibility, pcap open, monitor-like capture, datalink, channel switching, AP/SoftAP, frame injection, exposed PHY/security metadata. Persist results per Mac model + OS version. New `Sources/Capabilities/*`, `Sources/WaveDrivers/WaveDriverAirport*`, `Sources/Core/WaveHelper.m`, `Resources/Info.plist`.
- **Acceptance:** Runs on real Intel + Apple Silicon MacBooks and emits a persisted capability report with **no guessed status** (`macbook-hardware-validation-agent`). This report is what resolves every 🔍 `needs-probe` row in the parity matrix.
- **Outcome:** Implemented `Sources/Capabilities/KMHardwareProbe.{h,m}` + `KMHardwareCapability.{h,m}` (non-disruptive only, per safety), `NSLocationWhenInUseUsageDescription` added, probe runs async at launch and persists `~/Library/Application Support/KisMac3/hardware-probe-<model>-<os>.json`. Build green, app launches, report verified. **Scope decisions:** disruptive ops (monitor/channel-switch/AP/injection) are **reported `unknownRequiresActiveProbe`, not executed** (would drop the Wi-Fi link); did not touch `WaveDriverAirport*`/`WaveHelper.m` (the standalone probe didn't need them). **On Mac15,8 / macOS 27 / arm64:** Wi-Fi iface ✓, Location `permissionMissing` (NotDetermined → S1.2), CoreWLAN scan API-available (Location-gated), **2.4/5/6 GHz all supported**, PHY metadata ✓, **`pcap.open` permissionMissing (`/dev/bpf0` denied)** — live capture is BPF-permission-gated, not impossible. **Remaining:** Intel-MacBook validation + a future *consented active probe* to resolve the disruptive 🔍 rows (monitor/channel/AP/injection still `unknownRequiresActiveProbe`). Minor: `kCLAuthorizationStatusAuthorizedWhenInUse` macOS handling to confirm in S1.2.

### S1.2 — Info.plist permission strings + Location auth flow — ✅ DONE (`df49aba`)
- **Milestone:** 1/2 · **Impl:** macbook-capability-agent · **Rev:** macos-api-reviewer · **Val:** macbook-native-validation-agent · **Deps:** S0.5
- **Scope:** Add `NSLocationWhenInUseUsageDescription` (+ Bluetooth/Contacts strings as features land), wire `CLLocationManager` authorization with `locationManagerDidChangeAuthorization` and `didUpdateLocations:` (replacing the deprecated delegate in `GPSController.m`). Surface remediation when denied.
- **Acceptance:** Granting Location reveals BSSIDs in active scan; denial shows exact remediation steps; no silent failure. `macbook-native-validation-agent` records auth/blocked status.
- **Outcome:** `GPSController` modernized — `-requestWhenInUseAuthorization` (modern API) fired when the CoreLocation source starts; `-locationManagerDidChangeAuthorization:` (macOS 11+) handles all transitions; deprecated `didUpdateToLocation:fromLocation:` → `didUpdateLocations:`; denial surfaces an `NSAlert` with System-Settings remediation (one-time, non-nagging). New `KisMACLocationAuthorizationChanged` notification + `-locationAuthorizationStatus`/`-isLocationAuthorized` so the capability layer observes live auth. Fixed the S1.1 probe's macOS *WhenInUse* misreport (the symbol is unavailable on the macOS SDK → recognize granted via Always OR raw value 4, applied in both probe and GPSController). Build green; the two deprecated CoreLocation-delegate warnings are gone; runtime confirmed the change-handler fires and `kCLErrorDenied` routes correctly. Reviewer fix: added `#import <Foundation/Foundation.h>` to the touched `KisMACNotifications.h` (was relying on the prefix header). **Needs signed build + human TCC grant to confirm** the Allow dialog appears/persists and that granting reveals BSSIDs (signing = S8.x). Pre-existing IDE flag `GPSController.h:87` (`waypoint` typedef via include order) left for S0.5 header hygiene — not a regression.

### S1.3 — Capability engine core — ✅ DONE (`6302540`)
- **Milestone:** 3 · **Impl:** capability-engine-agent · **Rev:** architecture-invariants-reviewer · **Val:** capability-test-agent · **Deps:** S1.1
- **Scope:** Implement `KMCapability` + `KMCapabilityEngine` (`Sources/Capabilities/*`): the single source of truth for the Milestone 3 capability list. Every disabled feature exposes the exact reason (permission / unsupported hardware / unsupported adapter / macOS-blocked / parser-missing / scope-missing).
- **Acceptance:** `capability-test-agent`: UI cannot enable a feature unless the engine says it can; each capability returns a correct enabled/disabled + reason given a mocked probe result. Architecture invariant: no UI or driver decides hardware support directly.
- **Outcome:** `KMCapability.{h,m}` + `KMCapabilityEngine.{h,m}`. Designated init takes **injected** inputs (S1.1 `KMHardwareCapability` array + fail-closed `KMNoActiveScopeProvider` + `KMProtocolParserRegistry`); decision logic never calls the probe → hardware-independent + testable. All 29 Milestone-3 capabilities mapped with correct state+reason; active/offensive ops fail closed on `activeLabScopeMissing`; modern decoders `protocolParserMissing` (S2.1); external modules `unsupportedAdapter`; S3.x surfaces reported not-implemented. **Tests:** no XCTest target exists in this legacy pbxproj, so a `+runSelfTestReturningFailures:` harness covers 4 mocked scenarios (all-green / bpf-denied / no-wifi / scoped+parser) — **runs and PASSes** (verified independently via `KISMAC_CAP_SELFTEST=1`). Build green, app launches. **Boundary:** the engine establishes the contract; **rewiring UI/drivers to query it is S1.4**, and an adapter-presence input for radio ops extends in S4.2. A real XCTest target is a worthwhile later add (self-test only for now).

### S1.4 — Route active-scan plumbing through the engine — ✅ DONE (`84a93ad`)
- **Milestone:** 3/4 · **Impl:** capability-engine-agent · **Rev:** architecture-invariants-reviewer · **Val:** capability-test-agent · **Deps:** S1.3
- **Scope:** Make the existing CoreWLAN active scan (🔧) the first consumer of the capability engine; gate the scan button + driver selection on capability + Location.
- **Acceptance:** With Location granted and CoreWLAN available, scan runs; otherwise the action is disabled with a reason. No `needs-probe` feature is exposed as available.
- **Outcome:** `ScanController` owns a cached `KMCapabilityEngine` (built from a live probe on a background queue, rebuilt on `KisMACLocationAuthorizationChanged`). `-startScan` consults the engine for `KMFeatureScan` **and** the active driver's mapped capability (`-capabilityKeyForActiveDriver`: passive AirportExtreme→monitor, USB→injection, Kismet→remote, built-in→scan) and blocks with an `NSAlert`+`DBNSLog` if unavailable. `BridgeController` adds `-validateToolbarItem:`/`-validateMenuItem:` so the Scan affordance is disabled (not click-then-fail). **Architecture invariant holds**: the controller asks the engine, never hardcodes hardware checks. Runtime-verified end-to-end (probe→engine→gate): on this machine scan is gated `permissionMissing` ("Active scan requires Location authorization"), and the auth-change observer rebuilds the engine. Build green. Reviewer fix: added `#import <Cocoa/Cocoa.h>` to `ScanController.h` (cleared a 30+ standalone-analysis error cascade across ScanController/Private/Scriptable headers; remaining `NSDrawer` warnings are pre-existing → S5.x). **Deferred to S5.x:** full PrefsDriver UI gating (only minimal scan-time driver gating done here). USB driver detected via `NSClassFromString` to avoid pulling C++ `USBJack` into a `.m`.

---

## Wave 2 — Protocol & capture (depends on capability engine)

### S2.1 — Modern protocol metadata model + fixtures — ✅ DONE
- **Milestone:** 5/6 · **Impl:** wifi-protocol-agent · **Rev:** wireless-protocol-reviewer · **Val:** protocol-fixture-test-agent · **Deps:** S1.3
- **Scope:** Wi-Fi 4/5/6/6E/7 metadata model; WPA2/WPA3/OWE/Enterprise/PMF parsing; tagged-parameter parsing; hidden-SSID/malformed-frame handling. Build the `Tests/Fixtures/` pcap corpus (WEP, WPA2, WPA3, OWE, Enterprise, PMF, hidden, 6/6E/7 beacons).
- **Acceptance:** `protocol-fixture-test-agent`: every protocol claim has ≥1 fixture or a documented API limitation; malformed frames don't crash; display strings correct. No overclaiming live support where only imported-pcap support exists.
- **Outcome:** New `Sources/Core/KMProtocolMetadata.{h,m}` — a clean, **additive**, fully bounds-checked TLV walker that decodes RSN IE (id 48) + vendor WPA IE (id 221, MS OUI) → **WPA2 / WPA2-Enterprise / WPA3-SAE / WPA2-WPA3-Transition / WPA3-Enterprise / OWE / 802.1X-Enterprise** with **PMF** (RSN-caps MFPC/MFPR) posture, and HT(45)/VHT(191)/HE(ext 35)/6 GHz(ext 59)/EHT(ext 108) → **Wi-Fi 4/5/6/6E/7** with channel width (20/40/80/160/320 where present). Wired **additively** into the legacy path: `-[WavePacket parseTaggedData:]` decodes metadata over the same IE buffer (clamped to the real frame bound to avoid a legacy over-read that could be tricked by a spurious element in the tail), exposes `-protocolMetadata`; `-[WaveNet parsePacket:]` accumulates the strongest signal across frames and exposes `-securityString`/`-phyString`. It also refines the legacy `encryptionType` to WPA2 when an RSN IE is seen — the WEP/WPA2 path is augmented, not rewritten. **Parser registry:** `KMProtocolParserRegistry defaultRegistry` now registers `wpa3Decode`/`oweDecode`/`enterpriseDecode`/`wifi6Decode`, so the capability engine reports those four rows **available** (offline/imported-pcap decode — **not** a live-monitor claim). **Fixtures:** extended `Tests/Fixtures/make_pcap_fixture.py` (no deps, byte-reproducible) with 12 single-beacon `proto_*.pcap` fixtures (WEP, WPA2, WPA3-SAE, WPA3-transition, OWE, Enterprise, PMF-required, hidden-SSID, Wi-Fi 6/6E/7, and a malformed frame). **Self-test:** new `KMProtocolSelfTest` runs under `KISMAC_PROTO_SELFTEST=1` (mirrors S2.2): imports each fixture through the OFFLINE pipeline and asserts decoded security + PHY strings — **12/12 PASS**, malformed fixture proven not to crash; S1.3 + S2.2 self-tests still PASS (no regressions). Build **green** (clean Debug, Xcode 26.5 / macOS 27 / arm64, signing disabled). **Documented limitations** (`Tests/Fixtures/README.md`): HE/EHT channel width isn't parsed (width only from HT/VHT/EHT, so 6E shows no width); MLO/puncturing detected only as EHT presence; no live-capture claim.

### S2.2 — Imported pcap/pcapng as first-class offline mode — ✅ DONE
- **Milestone:** 7 · **Impl:** capture-backend-agent · **Rev:** capture-safety-reviewer · **Val:** capture-validation-agent · **Deps:** S1.3
- **Scope:** Wire a File-menu import action to `readPCAPDump:` (currently scriptable-only), verify pcapng, route parsed frames through the protocol model. Offline analysis must work with zero live-capture capability.
- **Acceptance:** Importing a golden pcap yields expected network/client counts (`capture-validation-agent`); pcapng verified or limitation documented.
- **Outcome:** The File→Import→**PCAP Dump** menu item was **already wired** in `MainMenu.xib` (id 909, `keyEquivalent="O"`) to `-[BridgeController importPCPFile:]` → `-[ScanController importPCAP:]` → `-[WaveScanner readPCAPDump:]` → `pcap_open_offline` — a working OFFLINE chain, but the open panel accepted **any** file. Hardened `importPCPFile:` to restrict to `pcap`/`pcapng`/`cap`/`dump` via `-setAllowedFileTypes:` (kept extension-based for consistency with the other importers + to avoid linking `UniformTypeIdentifiers.framework`; an early `allowedContentTypes`/`UTType` attempt failed to link). **pcapng works transparently** through the linked libpcap (verified — identical counts to pcap). **Golden fixtures** committed under `Tests/Fixtures/` (`golden.pcap` + `golden.pcapng`, byte-reproducible via `make_pcap_fixture.py`, no scapy dep; radiotap/`DLT_IEEE802_11_RADIO`; 3 beacons + 1 data frame → **3 networks / 7 clients**). **Headless self-test** `KMPcapImportSelfTest` runs at launch under `KISMAC_PCAP_IMPORT_SELFTEST` (default = bundled `golden.pcap`, or an explicit path): imports into a throwaway `WaveContainer` via the offline pipeline and logs PASS/FAIL with actual-vs-expected counts — **PASS on both pcap and pcapng**, no crash, **no live capture / radio / monitor / channel-switch** triggered (safety-confirmed in the launch log). Build green (Xcode 26.5 / macOS 27 / arm64, signing disabled). **Boundary:** modern-decode enrichment (Wi-Fi 6/6E/7, WPA3/OWE/PMF display metadata) is **S2.1**, not done here; imported frames route through the existing `WavePacket`→`WaveNet`→`WaveContainer` pipeline which already yields networks/clients/security.

### S2.3 — Crypto/cracking modernization — ✅ DONE
- **Milestone:** 6 · **Impl:** wifi-protocol-agent · **Rev:** wireless-protocol-reviewer · **Val:** protocol-fixture-test-agent · **Deps:** S0.5
- **Scope:** Migrate PMK/MIC/SHA1/MD5/MD4 from PolarSSL to CommonCrypto/CryptoKit (keep MD4/DES as LEAP attack primitives); fix the WPA 64-bit pointer-aliasing (`WaveNetWPACrack.m:115`), the weak-scheduling tautology guard (`ScanControllerScriptable.m:718`) and the `keyLen104` copy-paste bug; bounds-check wordlist readers.
- **Acceptance:** Each attack recovers the known key from a fixture capture; weak-scheduling path is reachable again; no buffer over-reads. (CPU-only; no hardware dependency.)
- **Outcome:** Completes an interrupted prior attempt (which had left the two reachability/copy-paste bug fixes uncommitted). The crack path no longer calls **PolarSSL**: all hash primitives moved to Apple **CommonCrypto** — `CC_MD5`/`CC_SHA1` in `WPA.m` (HMAC-MD5/SHA1, `F`/`PRF`/`wpaPasswordHash`), the WPA PMK in `WaveNetWPACrack.m` now uses `CCKeyDerivationPBKDF` (kCCPBKDF2 / kCCPRFHmacAlgSHA1, 4096 iters, SSID salt), `CC_MD4` for LEAP `NtPasswordHash` in `LEAP.m`, and `CC_MD5` for the WEP "Apple" wordlist in `WaveHelper.m` (`WirelessCryptMD5`). DES (`DESSupport.c`) kept as a LEAP attack primitive. **4 bugs fixed:** (1) weak-scheduling tautology guard `||`→`&&` `ScanControllerScriptable.m:766`; (2) `keyLenAll` copy-paste `keyLen40bit`→`keyLen104bit` `WaveNetWEPWeakCrack.m:101`; (3) WPA strict-aliasing UB — the hand-rolled `fastF`/`prepared_hmac_sha1` that XOR-ed the 20-byte digest through `(NSInteger*)` casts was **deleted** when the PMK moved to `CCKeyDerivationPBKDF`, so the aliasing is gone, not just patched; (4) wordlist reader bounds — `fgets` return now checked and the `strlen(wrd)-1` underflow on empty lines guarded in both `WaveNetWPACrack.m` and `WaveNetLEAPCrack.m`. **PolarSSL residual:** still referenced by the Xcode project / WEP weak-IV (AirCrack) engine and not removed from the pbxproj (out of scope, riskier); only the crack-path *calls* were eliminated. Acceptance via env-gated self-test `KISMAC_CRYPTO_SELFTEST=1` (mirrors S2.1/S2.2): CC_MD4/CC_MD5/CC_SHA1 digests, the WPA PMK (IEEE/“password” vector) on **both** the reference and migrated PBKDF2 paths (asserted equal), and a LEAP NtPasswordHash + DES round-trip — all **PASS**. Build **green**; no regressions (proto 12/12, import 3 nets/7 clients, cap self-test PASS).

### S2.4 — Kismet remote/drone backend modernization — ✅ DONE_WITH_CONCERNS
- **Milestone:** 7 · **Impl:** capture-backend-agent · **Rev:** capture-safety-reviewer · **Val:** capture-validation-agent · **Deps:** S1.3
- **Scope:** Rewrite `WaveDriverKismet*` raw sockets onto Network.framework; verify/refresh the protocol against a modern Kismet; add `com.apple.security.network.client`.
- **Acceptance:** Connects to a modern Kismet server in a lab and ingests networks (`capture-validation-agent`); plaintext-only, no silent capture failure.
- **Outcome:** New `Sources/WaveDrivers/KMKismetTransport.{h,m}` — a **Network.framework `NWConnection`** transport (plaintext TCP) that replaces the raw BSD `socket()`/`connect()`/blocking `read()` in **both** Kismet drivers. It bridges NWConnection's push model to the drivers' pull model via a thread-safe buffer + bounded `-readUpTo:into:timeout:`. **No silent failure:** `-connectWithTimeout:` returns NO with a populated `-lastError`/`-lastErrorReason` on refused/unreachable/timeout, and a dropped connection sets `isFailed` so the pull loop stops instead of hanging. `WaveDriverKismet.m` (the `*NETWORK:` text protocol) and `WaveDriverKismetDrone.m` (the binary drone stream) now connect/read through the transport and surface errors via **`NSAlert`** (replacing the deprecated `NSRunCriticalAlertPanel`), always also `DBNSLog`-ing. **Bugs fixed:** (1) drone `memset(&drone_sock,0,…)`-AFTER-set (the just-assigned `sin_addr` was zeroed → connect targeted `0.0.0.0`) — the whole hand-rolled sockaddr/bind path is gone (`WaveDriverKismetDrone.m` `-startedScanning`); (2) IPv4-only `inet_addr` → `nw_endpoint_create_host` (resolves names + IPv4/IPv6); (3) blocking `read()`/`select()`/`fd_set` on the main capture flow → bounded transport reads via `-fillBuffer:received:need:` (`-nextFrame`), which returns NO on a closed connection (no spin/hang); (4) the over-broad `@try` in drone `-startedScanning` that reported "No host/port set" on **any** exception, masking real causes — removed; (5) drone payload clamp to `sizeof(databuf)` + `ctrl.len` clamp to bytes actually received (no over-read). **Capability gating:** `kismetRemoteCapture` in `KMCapabilityEngine` flipped from "not modernized (unknownRequiresActiveProbe)" to **available** as a *user-initiated remote source* (independent of local radio/permission; connect is attempted only when the user starts the driver and fails closed with a reason). Driver start still routes through `-[ScanController capabilityKeyForActiveDriver]`→engine (S1.4). **Validation (mock server, headless):** new `Tests/Fixtures/mock_kismet_server.py` (no deps) speaks the legacy plaintext `*NETWORK:` protocol on 127.0.0.1 with two known fixtures (`00:11:22:33:44:55`/ch6, `AA:BB:CC:DD:EE:FF`/ch11); new env-gated `KMKismetSelfTest` (`KISMAC_KISMET_SELFTEST=1`) launches it and asserts (a) the modernized transport connects + `WaveDriverKismet` **ingests both networks**, and (b) a refused/dead-port connection **fails closed** with a reason in bounded time — **both PASS** (verified). Build **green** (Xcode 26.5 / macOS 27 / arm64, signing disabled; `Network.framework` linked). **No regressions:** `KISMAC_CAP_SELFTEST` PASS (incl. new Kismet assertions), `KISMAC_PROTO_SELFTEST` 12/12, `KISMAC_CRYPTO_SELFTEST` ALL PASS, `KISMAC_PCAP_IMPORT_SELFTEST` 3 nets/7 clients PASS.
  - **⚠️ HONEST CONCERN / deferred — modern Kismet 3.x:** these are the **legacy ~2006 protocols** (`*NETWORK:` text line protocol + `STREAM_DRONE_VERSION 9` binary drone stream). They will **NOT** talk to modern **Kismet 3.x** (REST/websocket/JSON over TLS). The transport was modernized and the bugs fixed, but the **protocol was NOT refreshed against a live modern server** (none available). A **modern Kismet 3.x protocol adapter is a separate future slice that requires a live Kismet server to validate** — do not claim modern-Kismet compatibility.
  - **Entitlement note:** `com.apple.security.network.client` was **NOT** added — it only matters under the **App Sandbox**, and this app is currently **unsandboxed** (so outbound connections work without it). If/when sandboxing lands (**S8.x**), that entitlement must be added for the Kismet client. Documented, not applied here (sandboxing is out of S2.4 scope).

---

## Wave 3 — Native MacBook surfaces (Milestone 2 breadth)

Each is an independent slice; **Impl:** macbook-native-surface-agent · **Rev:** privacy-reviewer · **Val:** macbook-native-validation-agent · **Deps:** S1.3.

### S3.1 — BLE inventory — ✅ DONE
- **Milestone:** 2 · **Impl:** macbook-native-surface-agent · **Rev:** privacy-reviewer · **Val:** macbook-native-validation-agent · **Deps:** S1.3
- **Scope:** CoreBluetooth advertisement scan + GATT for user-selected device. *Accept:* pass/fail/permission-blocked status; Bluetooth usage string + auth.
- **Outcome:** New `Sources/Bluetooth/` (added to the KisMac2 target + `CoreBluetooth.framework` linked). **Model:** `KMBluetoothDevice` (identifier UUID, local name, advertised service UUIDs, manufacturer data + parsed 16-bit company id, RSSI, connectability, observation count, first/last-seen; default `-description` is REDACTED + `-redactedDescriptionWithSettings:` via S4.1 `KMRedactionSettings`) and `KMGATTProfile`/`KMGATTService`/`KMGATTCharacteristic`. **Scanner:** `KMBluetoothScanner` wraps `CBCentralManager` — passive advertisement scan (`scanForPeripheralsWithServices:nil`, de-duped per identifier), `start/stop`, `-discoveredDevices` accessor, a delegate **and** `KMBluetoothScannerDidUpdateDevicesNotification`/`…AuthorizationChangedNotification`. **Auth:** class-level `+[KMBluetoothScanner currentAuthorizationState]` (no radio spin / no prompt) mapped to authorized/denied/notDetermined/restricted + remediation; `NSBluetoothAlwaysUsageDescription` added to `Info.plist`. **GATT:** `-enumerateGATTForDeviceIdentifier:timeout:completion:` connects + discovers services/characteristics for a **USER-SELECTED device only — NEVER auto-connects** (privacy/safety; interactive picker is S5.x). **Capability wiring:** new injectable `KMBluetoothPermissionProviding` input on `KMCapabilityEngine` (real impl `KMBluetoothPermissionProvider`, fail-closed default NOT-authorized), injected by `-[ScanController capabilityEngine]`/`-rebuildCapabilityEngine`. `bleScan`/`bleGattEnumeration` now resolve honestly: **authorized ⇒ available; denied/notDetermined ⇒ permissionMissing; restricted ⇒ macOSBlocked** — no longer "not implemented". **On Mac15,8 / macOS 27 / arm64:** live CB auth = `notDetermined` ⇒ `bleScan = unavailable/permissionMissing`. **Self-test** `KISMAC_BLE_SELFTEST=1` (`KMBluetoothSelfTest`): scanner init + authorization read/map + engine bleScan/bleGatt 3-way gating across mocked authorizations **and** this machine's live auth + model redaction — **all PASS**; a brief live advertisement scan runs only when authorized. Build **green** (Xcode 26.5 / macOS 27 / arm64, signing disabled). **No regressions:** `KISMAC_CAP_SELFTEST` PASS, `KISMAC_SAFETY_SELFTEST` PASS, `KISMAC_ACTIVEGATE_SELFTEST` PASS, `KISMAC_PROTO_SELFTEST` 12/12, `KISMAC_CRYPTO_SELFTEST` ALL PASS, `KISMAC_PCAP_IMPORT_SELFTEST` 3 nets/7 clients. **Needs a signed build + a human Bluetooth grant** to exercise actual advertisements + GATT connect/enumerate (cannot be granted headlessly for an unsigned ad-hoc binary). **Deferred:** S5.x interactive device-selection UI + Bluetooth workspace; BLE privacy analysis (address-rotation/beacon-ID/proximity trends) and report export are later Milestone-2 items.
### S3.2 — USB/HID/serial inventory — ✅ DONE
- **Milestone:** 2 · **Impl:** macbook-native-surface-agent · **Rev:** privacy-reviewer · **Val:** macbook-native-validation-agent · **Deps:** S1.3
- **Scope:** USB/HID/serial inventory (IOKit, read-only). *Accept:* enumerates VID/PID/class/serial; serial pucks discoverable for GPS.
- **Outcome:** New `Sources/HardwareInventory/` (added to the KisMac2 target; IOKit.framework already linked). **Models:** `KMUSBDevice` (idVendor/idProduct, vendor+product string descriptors, bDeviceClass/SubClass, USB serial, IOKit locationID + IORegistry service path, negotiated Device Speed → low/full/high/super/super+), `KMHIDDevice` (primary usagePage/usage → derived category Keyboard/Pointing device/Game controller/Consumer-remote, VID/PID, product, manufacturer, transport, serial), `KMSerialDevice` (IOTTYBaseName, /dev/cu.* callout + /dev/tty.* dialin paths, suffix, `looksLikeUARTOrGPS` heuristic for USB-UART/GPS pucks). All three expose `dictionaryRepresentation` + `redactedDescriptionWithSettings:` (default `-description` redacted). **Collector:** `KMHardwareInventory` — one-shot `enumerate{USB,HID,Serial}Devices` via `IOServiceMatching(kIOUSBDeviceClassName)` (IOUSBHostDevice fallback) / `kIOHIDDeviceKey` / `kIOSerialBSDServiceValue`, reading registry properties only (`IORegistryEntrySearchCFProperty`). **READ-ONLY / no permission:** never `IOServiceOpen`/claim/write/configure any device — pure diagnostics; macOS needs no authorization for registry reads, so the engine reports these unconditionally available. **Privacy:** serials are stable hardware identifiers and are treated as sensitive — masked via the S4.1 `KMRedactionSettings` (Bluetooth-identifier transform); device paths are not personal identifiers so they stay intact. **Capability wiring:** `usbInventory`/`hidInventory`/`serialInventory` flipped from `unknownRequiresActiveProbe` "not implemented (S3.x)" to **available**; LAN/Bonjour rows stay `unknownRequiresActiveProbe` (S3.3). **Self-test** `KISMAC_USBINV_SELFTEST=1` (`KMHardwareInventorySelfTest`): REALLY enumerates THIS machine (IOKit needs no permission), logs counts + a redacted sample, asserts enumeration doesn't crash, HID > 0, serial not dumped raw, and engine usb/hid/serialInventory available — **all PASS**. **On Mac15,8 / macOS 27 / arm64:** usb=10, hid=30, serial=3 live. Build **green** (Xcode 26.5 / macOS 27 / arm64, signing disabled). **No regressions:** `KISMAC_CAP_SELFTEST` PASS (incl. new usb/hid/serial available assertions), `KISMAC_BLE_SELFTEST` PASS, `KISMAC_SAFETY_SELFTEST` PASS, `KISMAC_ACTIVEGATE_SELFTEST` PASS, `KISMAC_PROTO_SELFTEST` 12/12, `KISMAC_CRYPTO_SELFTEST` ALL PASS, `KISMAC_PCAP_IMPORT_SELFTEST` 3 nets/7 clients PASS. **Deferred:** S5.x USB/HID/serial workspace UI + interactive inventory views; optional IOKit hotplug (matching/terminated) notifications (one-shot enumeration is the must-have and is done).
### S3.3 — LAN discovery — ✅ DONE
- **Milestone:** 2 · **Impl:** macbook-native-surface-agent · **Rev:** privacy-reviewer · **Val:** macbook-native-validation-agent · **Deps:** S1.3
- **Scope:** LAN discovery (Bonjour/mDNS/DNS-SD passive; opt-in ping sweep, rate-limited). *Accept:* service inventory on the joined network; no stealth behavior; privacy review of host/IP handling.
- **Outcome:** New `Sources/LAN/` (added to the KisMac2 target; `Network.framework` already linked from S2.4). **Model:** `KMLANService` (instance name, DNS-SD type, domain, resolved host/port/IP addresses, `isResolved`, dedup `identityKey`; `dictionaryRepresentation` + S4.1-aware `redactedDescriptionWithSettings:`; default `-description` fully redacted). **Manager:** `KMLANDiscovery` — passive discovery on the **modern `nw_browser`** (Network.framework) over a curated 20-type set (`_http`/`_https`/`_ssh`/`_smb`/`_afpovertcp`/`_ipp`/`_airplay`/`_raop`/`_companion-link`/`_rfb`/`_homekit`/`_googlecast`/`_workstation`/… all `._tcp`, domain `local.`). `start/stopBrowsing`, `discoveredServices` (deduped by type+name+domain, most-recent-first), a delegate **and** `KMLANDiscoveryDidUpdateServicesNotification`. Each discovered instance is resolved to host/port/IP via a short-lived `nw_connection` that is **cancelled at `ready` before any application data is sent** (passive resolution, not a probe). All mutation runs on the manager's own serial queue (so result collection never depends on the main runloop draining — avoids stalling behind a modal). **PASSIVE ONLY / NO STEALTH:** only listens to mDNS announcements the OS already receives; `-pingSweepIsImplemented` honestly returns **NO** (the opt-in, default-OFF, rate-limited active sweep is a documented future hook, deliberately NOT implemented here). **Local Network privacy:** `NSLocalNetworkUsageDescription` (honest: passive local service inventory for authorized auditing, local-only) + `NSBonjourServices` (the 20 browsed types) added to `Info.plist`; macOS exposes no queryable Local-Network auth API, so the manager surfaces a best-effort posture via `-localNetworkState` (unknown→active once results flow, denied if the browser fails). **Privacy:** instance names/hostnames masked via S4.1 `redactHostname:`; IPs masked under the `redactHostnames` toggle (IPv4 keeps the first octet, IPv6 a short prefix); local-only. **Capability wiring:** `lanDiscovery`/`bonjourInventory` flipped from `unknownRequiresActiveProbe` "not implemented (S3.x)" to **available** (passive Bonjour is always permitted to attempt; if the user denies Local Network the OS silently returns no results and the manager reports that at runtime). **Self-test** `KISMAC_LAN_SELFTEST=1` (`KMLANSelfTest`): instantiates the browser, runs a brief ~3s passive browse on THIS machine, logs the discovered count + a REDACTED sample + `localNetworkState`, and asserts the browser starts/stops cleanly, the engine reports lanDiscovery/bonjourInventory **available** (or permissionMissing — either an honest PASS), hostnames are redactable, and no active/stealth path exists — **all PASS**. **On Mac15,8 / macOS 27 / arm64 (unsigned ad-hoc):** browser `state=active`, services=0 (Local Network TCC grant requires a signed/registered app; live results are signing-gated, as noted in the test output). Build **green** (Xcode 26.5 / macOS 27 / arm64, signing disabled). **No regressions:** `KISMAC_CAP_SELFTEST` PASS (updated: LAN/Bonjour now assert available), `KISMAC_USBINV_SELFTEST` usb=10/hid=30/serial=3 PASS, `KISMAC_BLE_SELFTEST` PASS, `KISMAC_SAFETY_SELFTEST` PASS, `KISMAC_ACTIVEGATE_SELFTEST` PASS, `KISMAC_PROTO_SELFTEST` 12/12, `KISMAC_CRYPTO_SELFTEST` ALL PASS, `KISMAC_PCAP_IMPORT_SELFTEST` 3 nets/7 clients PASS. **Needs a signed build + a human Local Network grant** to enumerate real services. **Deferred:** opt-in rate-limited active ping sweep (future slice, gated behind authorized scope); S5.x LAN/Bonjour workspace UI + interactive service views; S3.4 local-interface inventory.
### S3.4 — Local interface inventory — ✅ DONE — Wave 3 native surfaces complete
- **Milestone:** 2 · **Impl:** macbook-native-surface-agent · **Rev:** privacy-reviewer · **Val:** macbook-native-validation-agent · **Deps:** S1.3
- **Scope:** Local interface inventory (Wi-Fi/Ethernet/Thunderbolt/VPN/AWDL/loopback + IP config). *Accept:* matches `ifconfig`/system for a known setup.
- **Outcome:** New files under `Sources/LAN/` (added to the KisMac2 target; `SystemConfiguration.framework` linked — it was not previously). **Model:** `KMNetworkInterface` (BSD name; classified `KMInterfaceType` Wi-Fi/Ethernet/Thunderbolt-bridge/VPN/AWDL-peer/loopback/other; up/running/broadcast/p2p/loopback flags + raw flag bitfield; link-layer MAC where present; IPv4/IPv6 address lists + IPv4 netmasks; `isPrimary`; `hasIPAddress`; `dictionaryRepresentation` + S4.1-aware `redactedDescriptionWithSettings:`; default `-description` fully redacted). **Collector:** `KMInterfaceInventory` — one-shot `+currentInterfaces` over **`getifaddrs(3)`**, coalescing the per-family ifaddrs entries (AF_LINK/AF_INET/AF_INET6) into one model per BSD name, ordered primary→up-with-IP→rest. `+primaryInterfaceName` reads SystemConfiguration's `State:/Network/Global/IPv4` `PrimaryInterface` (the default-route iface); `+wiFiInterfaceName` cross-checks CoreWLAN's default interface name to harden the en* Wi-Fi-vs-Ethernet classification. **Classification heuristics:** `lo*`/IFF_LOOPBACK→loopback; `utun*`/`ipsec*`/`ppp*`/`tun*`/`tap*`/`gif*`/`stf*`→VPN; `awdl*`/`llw*`→AWDL/peer; `bridge*`→Thunderbolt-bridge; `en*` → CoreWLAN-confirmed Wi-Fi wins, else en0=Wi-Fi (MacBook default) / en1=Ethernet / en[2-9]+=Thunderbolt-bridge heuristic. **READ-ONLY / LOCAL-ONLY:** reads the kernel interface table + SCDynamicStore global state only — never configures an interface, sends traffic, or probes a host; macOS needs no authorization, so this is an always-available read-only data feature (no Milestone-3 capability row exists for it, and none was invented per scope note 3). **Privacy:** the link-layer MAC is masked via the S4.1 MAC transform (OUI kept) and IPs via the hostname/IP transform (first octet / IPv6 prefix kept), mirroring `KMLANService`; default `-description` is fully redacted (safe for logs). **Self-test** `KISMAC_IFACE_SELFTEST=1` (`KMInterfaceInventorySelfTest`): enumerates THIS machine, logs a REDACTED per-interface summary (name/type/up/has-IP, MAC/IP masked), and asserts enumeration doesn't crash, `lo0` present + loopback-classified + up, ≥1 `en*` present, ≤1 primary flagged and the flagged primary matches SystemConfiguration's `PrimaryInterface` (or none flagged when offline), the coalesced name set equals the `getifaddrs` ground truth, and no raw MAC leaks into the redacted description — **all PASS**. **On Mac15,8 / macOS 27 / arm64:** 33 interfaces (==`ifconfig -l` count), primary=`en10` (matches `route -n get default` and SystemConfiguration), en0=Wi-Fi (CoreWLAN-confirmed), awdl0/llw0=AWDL, utun*=VPN, bridge0/en[2-6]=Thunderbolt-bridge, lo0=loopback. Build **green** (Xcode 26.5 / macOS 27 / arm64, signing disabled). **No regressions:** `KISMAC_CAP_SELFTEST` PASS, `KISMAC_LAN_SELFTEST` PASS, `KISMAC_USBINV_SELFTEST` usb=10/hid=30/serial=3 PASS, `KISMAC_BLE_SELFTEST` PASS, `KISMAC_SAFETY_SELFTEST` PASS, `KISMAC_ACTIVEGATE_SELFTEST` PASS, `KISMAC_PROTO_SELFTEST` 12/12, `KISMAC_CRYPTO_SELFTEST` ALL PASS, `KISMAC_PCAP_IMPORT_SELFTEST` 3 nets/7 clients PASS. **Deferred:** S5.x interface/LAN workspace UI + interactive views; optional SCDynamicStore live-change notifications (one-shot inventory is the must-have and is done).

---

## Wave 4 — Safety guardrails (gate for all active features)

### S4.1 — Campaign scope + audit log + emergency stop — ✅ DONE
- **Milestone:** 9 · **Impl:** privacy-safety-agent · **Rev:** security-abuse-reviewer · **Val:** safety-policy-test-agent · **Deps:** S1.3
- **Scope:** `Sources/Safety/*`, `Sources/Campaigns/*`: authorized-campaign model with scope (SSIDs/BSSIDs/clients/adapters/channels/time window), always-visible emergency stop, audit log, redaction settings, server-export opt-in (default off), local-only storage, project encryption option.
- **Acceptance:** `safety-policy-test-agent`: active features **fail closed** without scope; emergency stop halts; audit entries recorded; redaction applied. Mandatory `security-abuse-reviewer` sign-off.
- **Outcome:** New `Sources/Campaigns/` + `Sources/Safety/`. **Model:** `KMCampaign` (explicit `authorized` flag + `authorizedBy` attribution + emergency-stop latch) holds a `KMCampaignScope` { SSIDs, BSSIDs, clientMACs, adapters, channels, windowStart/End }; `-[KMCampaign isActive]` is the single source of truth = **authorized AND not emergency-stopped AND within time window** (MAC sets normalized — case/separator-insensitive; empty collection = nothing in scope, never "everything"; both value objects are `NSSecureCoding`). **Real provider:** `KMActiveCampaignScopeProvider` implements the engine's `<KMActiveScopeProviding>` (`-hasActiveScope` = an active campaign exists) plus membership queries (`scopeContainsBSSID:`/`ClientMAC:`/`SSID:`/`Channel:`/`Adapter:`) — all fail closed when no active scope. `KMCampaignManager` (singleton, or inject an audit log for tests) owns the current campaign and vends the provider. **Wiring:** added `-[KMCapabilityEngine initWithProbe:scopeProvider:]`; `-[ScanController capabilityEngine]` (lazy) **and** `-rebuildCapabilityEngine` now inject `[KMCampaignManager sharedManager].scopeProvider` instead of the default `KMNoActiveScopeProvider`, so apMode/frameInjection/deauth are gated on a real authorized in-window campaign — **fail closed by default (no campaign → no scope → `activeLabScopeMissing`)**. A new `KMActiveScopeChangedNotification` observer rebuilds the engine on arm/disarm/e-stop (mirrors the S1.2 Location observer). **Emergency stop:** `-[KMCampaignManager engageEmergencyStop:]` is always callable (even with no campaign), latches `emergencyStopped`, forces the current campaign inactive (so every active op fails closed at once), records an `emergency_stop` audit entry, and posts `KMEmergencyStopEngagedNotification` + `KMActiveScopeChangedNotification` for UI binding (the always-visible UI affordance is **S5.x**; state+method+notification are here). **Audit log:** `KMAuditLog` — append-only `.jsonl` at `Application Support/KisMac3/audit-YYYY-MM-DD.jsonl`, serial-queue writes, never silently drops (write failure → `DBNSLog` + unencodable-entry fallback line), readable back via `-readCurrentEntries`; optional redaction of identifier fields at write time (metadata never redacted). **Redaction:** `KMRedactionSettings` — master switch (default OFF for parity) + independent MAC/SSID/hostname/Bluetooth toggles; OUI-preserving MAC mask, ends-preserving SSID, TLD-preserving hostname, free-text MAC masking via regex; `NSSecureCoding`/`NSCopying`. **Privacy defaults:** `KMPrivacySettings` (NSUserDefaults-backed) — `storeLocally`=YES, `serverExportEnabled`=**NO** (opt-in; `-allowsServerExport` fails closed), `projectEncryptionEnabled`=NO (flag + documented S6.x path only — no crypto here), embedded redaction settings. **Self-test:** `KMSafetySelfTest` under `KISMAC_SAFETY_SELFTEST=1` builds the REAL provider into a real engine over MOCKED all-green hardware and asserts all 6 acceptance scenarios — **35/35 PASS** (no campaign → fail closed; armed in-window → scope ON, gate flips to `unsupportedMacBookHardware` per S1.3; e-stop → fail closed + audit entry; expired window → not active; audit appended/readable; redaction on/off). Build **green** (Xcode 26.5 / macOS 27 / arm64, signing disabled). **No regressions:** `KISMAC_CAP_SELFTEST` PASS, `KISMAC_PROTO_SELFTEST` 12/12, `KISMAC_CRYPTO_SELFTEST` ALL PASS, `KISMAC_PCAP_IMPORT_SELFTEST` 3 nets/7 clients. **No turnkey abuse path:** this slice adds scope/audit/stop/redaction machinery only — inject/deauth/AP stay gated and built-in hardware still can't do them; actually gating the 🔌 active features behind capability+scope is **S4.2**. **Deferred:** S5.x always-visible emergency-stop UI + campaign editor; S6.x project encryption crypto + report-wide redaction application.

### S4.2 — Gate inject/deauth/AP behind capability + scope — ✅ DONE
- **Milestone:** 9 · **Impl:** privacy-safety-agent · **Rev:** security-abuse-reviewer + capture-safety-reviewer · **Val:** safety-policy-test-agent · **Deps:** S4.1, S1.1
- **Scope:** Wire the 🔌 `hardware-required` active features (inject/deauth, and any probe-confirmed monitor/AP) so they require both a capability=true and an authorized campaign scope.
- **Acceptance:** Without adapter → disabled with reason; with adapter but no scope → disabled; with both → allowed and audited. Tests fail closed.
- **Outcome:** Added the **adapter-presence input** the engine lacked at S1.3 (S1.3 explicitly deferred it here). New `@protocol KMAdapterPresenceProviding { -hasInjectionCapableAdapter }` + a fail-closed default `KMNoAdapterPresenceProvider` (reports **NO** — the built-in card can't transmit) in `KMCapabilityEngine.{h,m}`, and a **real** impl `Sources/Capabilities/KMWaveDriverAdapterPresenceProvider.{h,m}` backed by `-[WaveHelper getWaveDrivers]` / `-[WaveDriver allowsInjection]` (YES only when an external `WaveDriverUSB`-class adapter is loaded). The engine's designated init now takes `adapterProvider:`, and the **offensive trio** (`frameInjection`/`deauth`/`apMode`) decision became a **3-way**: no injection-capable adapter ⇒ `unsupportedAdapter`; adapter but no scope ⇒ `activeLabScopeMissing`; adapter **and** authorized in-window scope ⇒ **available**. `monitorMode`/`channelSwitching`/`handshakeCapture` left as `unknownRequiresActiveProbe` (unchanged). Both `-[ScanController capabilityEngine]` and `-rebuildCapabilityEngine` now inject the real adapter provider alongside the S4.1 scope provider. **Op-site hard gate (defense in depth):** new `-[ScanController allowActiveOperation:feature:target:targetIsClient:]` requires (1) the engine reports the feature **available** (⇒ adapter + scope) else refuses with the engine's reason (NSAlert + DBNSLog) and audits the refusal; (2) the **target is in campaign scope** — `scopeContainsBSSID:` for an AP, `scopeContainsClientMAC:` for a targeted client — out-of-scope ⇒ refuse + audit even with adapter+scope; (3) on allow, records a `KMAuditLog` active-operation entry **before** execution. Wired into the ACTUAL op entry points in `WaveScanner.mm`: `-tryToInject:` (BSSID), `-deauthenticateNetwork:atInterval:` (BSSID), `-injectionTest:withClient:` (client MAC if targeted, else BSSID), `-setDeauthingAll:` (broadcast deauth-all passes a **nil** target ⇒ fails closed: a blanket deauth-all can't be constrained to one scoped BSSID, so it's refused+audited until a future slice adds explicit broadcast authorization). **No transmit on built-in HW and no turnkey path** — this is the GATE only; real injection still needs a real USB adapter (deferred, hardware). **Self-test** `KMActiveGateSelfTest` (`KISMAC_ACTIVEGATE_SELFTEST=1`) uses a MOCK adapter + the real S4.1 scope provider + real engine and asserts the full matrix — **all PASS** (no adapter/no scope ⇒ `unsupportedAdapter` + NOT available; adapter/no scope ⇒ `activeLabScopeMissing`; adapter + in-window scope ⇒ `available`; adapter+scope but target BSSID/client **out of scope** ⇒ op-guard refuses (`scopeContains*` NO) + **audited**; emergency stop ⇒ refused again + audited). Build **green** (Xcode 26.5 / macOS 27 / arm64, signing disabled). **No regressions:** `KISMAC_CAP_SELFTEST` PASS (updated for the adapter 3-way + new adapter-present scenario), `KISMAC_SAFETY_SELFTEST` PASS (updated: armed+adapter ⇒ trio available), `KISMAC_PROTO_SELFTEST` 12/12, `KISMAC_CRYPTO_SELFTEST` ALL PASS, `KISMAC_PCAP_IMPORT_SELFTEST` 3 nets/7 clients. **Deferred:** real USB-adapter transmit (needs hardware); always-visible emergency-stop UI + campaign editor (S5.x); explicit broadcast-deauth authorization model.

---

## Wave 5 — UI, reporting, lab features, release

Outlined (expanded into slices when their dependencies are near):

- **S5.x — Modern macOS UI** (Milestone 8): sidebar/toolbar/dashboard, capability dashboard, inspector, permission/unsupported states. *Impl:* modern-macos-ui-agent · *Rev:* macos-hig-reviewer · *Val:* ui-smoke-test-agent · *Deps:* S1.3 + the data models. Includes porting traffic graph off deprecated OpenGL/BIGL and map off BIGeneric.
- **S6.x — Project/report/export** (Milestone 12): `.kismac` bundle with secure coding, unified report, evidence timeline with source labels, redaction/encryption. *Impl:* project-reporting-agent · *Rev:* privacy-reviewer + architecture-invariants-reviewer · *Val:* report-export-test-agent · *Deps:* S4.1. **First slice S6.1 ✅ DONE (below).**

### S6.1 — Secure `.kismac` load + unified report generator — ✅ DONE
- **Milestone:** 12 · **Impl:** project-reporting-agent · **Rev:** privacy-reviewer + architecture-invariants-reviewer · **Val:** report-export-test-agent · **Deps:** S4.1
- **Scope:** (A) Harden the insecure legacy `.kismac` unarchiver (the documented S6.x security debt). (B) New `Sources/Reporting/` unified report generator covering the Milestone-12 sections + an evidence timeline with source labels, applying S4.1 redaction + honoring S4.1 privacy.
- **Acceptance:** `KISMAC_REPORT_SELFTEST=1` — Part A secure round-trip + malformed/hostile-input fail-closed; Part B golden.pcap → report with all sections + the 3 known networks + capability/blocked section + redaction ON⇒no raw BSSID/SSID / OFF⇒present. Build green; full self-test suite no regressions.
- **Outcome:** **Part A (secure load):** the legacy fallback's `-unarchiveObjectWithData:`/`-unarchiveObjectWithFile:` in `Sources/Core/WaveStorageController.m` (deserialized **arbitrary classes** from an untrusted file) are replaced with `+[NSKeyedUnarchiver unarchivedObjectOfClasses:fromData:error:]` via a new `+secureLegacyDictionaryFromData:` + `+secureLegacyAllowedClasses` helper. The **allowlist** is exactly the classes the format legitimately contains: `NS{Mutable,}Dictionary`/`NS{Mutable,}Array`/`NS{Mutable,}String`/`NS{Mutable,}Data`/`NSDate`/`NSNumber` + the **only two** KisMAC model classes coded into the graph — **`WaveNet`** and **`WaveClient`**, which now **adopt `NSSecureCoding`** (`+supportsSecureCoding`→YES; `-initWithCoder:` rewritten from `decodeObjectForKey:` to typed `decodeObjectOfClass:forKey:` / `decodeObjectOfClasses:forKey:` — incl. the nested `aClients` dict `{NSString: WaveClient}`; a **symmetric `-encodeWithCoder:` was added** to both, since legacy KisMAC had dropped the encode side — needed for a well-formed secure-coding contract + the round-trip test). The modern compressed plist save path (`NSPropertyListSerialization`) was already safe and is **untouched** (it's the documented S6.x plist-modernization floor, separate). **Malformed/hostile input fails closed:** a disallowed root class (tested with `NSValue`), corrupt bytes, and empty data all return nil + a logged NSError — **no arbitrary-class instantiation, no crash**, never the old insecure path. **No residual insecure call remains** in the `.kismac` load path. **Part B (report):** new `Sources/Reporting/KMReport.{h,m}` — a generator that READS a populated `WaveContainer` + the `KMCapabilityEngine` + `KMHardwareProbe` + the S3.x inventory arrays (BLE/USB/HID/serial/LAN/interfaces) and renders structured **Markdown** with sections: discovered networks (with S2.1 `securityString`/`phyString`), observed clients, protocol/security posture, LAN hosts/services + local interfaces, BLE devices, USB/HID/serial inventory, capture/evidence files, **MacBook capability result** (from the probe), **Capabilities & unsupported/blocked features WITH REASONS** (copied verbatim from the engine — `KMStringFromCapabilityReason` + explanation), and an **evidence timeline** with explicit **source labels** (CoreWLAN / CoreBluetooth / pcap / Kismet / USB / user-import / manual-note), auto-deriving per-network first/last-seen rows tagged CoreWLAN (live) vs pcap (imported). **Architecture invariant holds:** the report never decides capability/hardware — it only renders the engine's output (the engine is an injected input). **Privacy (S4.1):** every identifier (BSSID/MAC, SSID, hostname, BLE id, USB serial) is passed through the injected `KMRedactionSettings`; inventory models render via their own `-redactedDescriptionWithSettings:`. When redaction is **ON no raw BSSID/SSID is emitted**; OFF emits them (parity). `KMPrivacySettings` honored: the report is a **local artifact** (`-writeMarkdownToFile:` is local-only; server export stays the opt-in/off guard at the caller); the header surfaces the local-only + server-export-OFF + redaction state. **Self-test** `KMReportSelfTest` under `KISMAC_REPORT_SELFTEST=1`: A1 WaveNet NSSecureCoding archive→secure-decode round-trip; A2 full `.kismac` legacy load through the secure path (raw `NSKeyedArchiver` dict written to a temp file → `loadFromFile:` falls through to the hardened legacy loader → container fidelity); A3 malformed/hostile (disallowed root / garbage / empty) all fail closed; B0 golden.pcap → 3 networks; B1 all report sections + 3 known SSIDs + source labels present; B2 redaction OFF emits raw SSID+BSSID; B3 redaction ON emits neither — **all PASS**. **pbxproj:** new `Reporting` group + `KMReport.m`/`KMReportSelfTest.m` added to the KisMac2 target (`plutil -lint` OK; an initial CA9013xx **ID collision with the SystemConfiguration.framework build entry** was caught at link time — `_OBJC_CLASS_$_KMReport` undefined — and fixed by renumbering to the free `CA9014xx` range). **0xA7 author byte** in `WaveNet.{h,mm}` + `WaveClient.{h,m}` was mangled by Edit (→ `ef bf bd`) and **byte-restored** to `0xA7`; `git diff` shows no author-line changes. Build **green** (clean Debug, Xcode 26.5 / macOS 27 / arm64, signing disabled). **No regressions** across the full suite (see Verify). **Deferred (remaining S6.x / S5.x):** **S6.2 — project encryption** — ✅ now DONE (`KMProjectCrypto` AES-256-CBC+HMAC-SHA256 EtM / PBKDF2 envelope wired into `WaveStorageController`; see S6.2 below); **S5.x — report-export UI** (HTML render, the File→Export-report menu action + the server-export opt-in UI; `KMReport` produces the artifact, wiring it to a menu/visible UI is S5.x); legacy plist `propertyListFromData:`/`dataFromPropertyList:` modernization (the documented S6.x deprecation floor) and `GBStorage`'s own `archivedDataWithRootObject:`/`unarchiveObjectWithData:` secure-coding migration remain.
### S6.2 — Project encryption option — ✅ DONE
- **Milestone:** 12 · **Impl:** project-reporting-agent · **Rev:** privacy-reviewer · **Val:** report-export-test-agent · **Deps:** S4.1, S6.1
- **Scope:** Build the crypto behind `KMPrivacySettings.projectEncryptionEnabled` (a flag-only stub before this slice): a passphrase-based authenticated-encryption envelope for the `.kismac` payload, wired into `WaveStorageController` save/load, local-only, passphrase never persisted.
- **Outcome:** New `Sources/Safety/KMProjectCrypto.{h,m}` — a **CommonCrypto-only** (no third-party deps) authenticated-encryption envelope. **KDF:** PBKDF2-HMAC-SHA256 from the operator passphrase + a random 16-byte salt, iteration count via `CCCalibratePBKDF` (~150 ms) **floored at 210000**, deriving **64 bytes → encKey(32) ‖ macKey(32)**. **Cipher:** the public CommonCrypto SDK on the deployment target (macOS 13) exposes AES + HMAC but **NOT the GCM SPI** (`CCCryptorGCMOneshot*` is SPI, absent from `CommonCryptor.h` on the Xcode 26.5 SDK), so the seal uses the standard, sound **Encrypt-then-MAC**: `AES-256-CBC(encKey, iv, PKCS7(plaintext))` then `tag = HMAC-SHA256(macKey, header ‖ iv ‖ ciphertext)`. **Envelope (versioned, magic header):** `magic(6 "KMENC1") ‖ ver(1) ‖ kdf(1) ‖ iterations(4 BE) ‖ salt(16) ‖ iv(16) ‖ ciphertext(n) ‖ tag(32)`. **Open** parses the envelope, re-derives keys, **verifies the HMAC (constant-time) BEFORE decrypting** — a wrong passphrase (→ wrong macKey) or any tamper (ciphertext/tag/header) fails the MAC and returns **nil + NSError (`KMProjectCryptoErrorAuthFailed`)**, never partial/garbage plaintext (CBC decrypt only runs post-auth). Salt+iv are freshly CSPRNG-random (`SecRandomCopyBytes`) every seal. **Wiring (`WaveStorageController`):** `+saveToFile:` — when `projectEncryptionEnabled` **AND** a session passphrase is set (`+shouldEncryptOnSave`), the existing `BICompressor` writes the gzip payload to a temp file which is then **sealed** into the KMENC1 envelope at the real path (`+sealWorkFile:toFile:`); otherwise behaviour is **exactly as before** (plain gzip). `+loadFromFile:` — `+decryptedWorkPathForFile:` detects the magic header (`+fileIsEncryptedEnvelope:`); an encrypted file requires the passphrase and is **decrypted to a temp file** (cleaned up at every exit) before the existing `BIDecompressor`/S6.1 secure-unarchive path; a non-encrypted file opens unchanged; missing/wrong passphrase or tamper **fails closed → NO**. **Passphrase handling (privacy invariant):** held in a process-memory static only (`+setSessionPassphrase:`/`+sessionPassphrase`/`+hasSessionPassphrase`/`+clearSessionPassphrase`) — **NEVER** written to disk, `NSUserDefaults`, the audit log, or any log line; no key material or plaintext-with-secrets is ever logged. **Pre-existing bug found + fixed (flagged):** `BICompressor.m` (`Subprojects/BIGeneric`, left unstaged) declared its per-field length prefix as **`NSUInteger sizeToWrite` (8 bytes on 64-bit)** while `BIDecompressor` reads **`uint32_t` (4 bytes)** — so the writer emitted 8-byte headers and the reader mis-aligned every field by 4 bytes, **silently truncating the creator string / data on EVERY load** (not just encrypted ones — the legacy `.kismac` round-trip was already broken on 64-bit). Fixed `addString:`/`addData:` to `uint32_t` so writer+reader agree; the full save→load round-trip now preserves the network set. **Self-test** `KISMAC_PROJCRYPTO_SELFTEST=1` (wired in `ScanController applicationDidFinishLaunching:`): Part 1 `+[KMProjectCrypto runSelfTestLogging]` — seal→open round-trip recovers identical plaintext; **WRONG passphrase → nil+authFailed (no leak)**; **TAMPERED ciphertext → authFailed**; **TAMPERED tag → authFailed**; versioned (magic+version byte); **salt AND iv differ across two seals of the same input**; envelope detection; empty-passphrase refused — **10/10 PASS**. Part 2 `+[WaveStorageController runProjectCryptoStorageSelfTestLogging]` — golden.pcap → 3 nets → encrypted save → on-disk file is a KMENC1 envelope → **open with right passphrase preserves 3/3 networks**; open with **WRONG** / **NO** passphrase fails cleanly (0 networks) — **ALL PASS** (restores the prior privacy flag + session passphrase, side-effect-free). **pbxproj:** `KMProjectCrypto.{h,m}` added to the `Safety` group + KisMac2 target in the free **CA9015xx** range (`plutil -lint` OK, no ID collision with framework build-file IDs). **0xA7/`ß` author bytes** in the touched legacy files (`WaveStorageController.{h,m}`, `ScanController.m`, `BICompressor.m`) **intact** — `git diff` shows no author-line changes; new files ASCII. Build **green** (fresh clean Debug, Xcode 26.5 / macOS 27 / arm64, signing disabled; only the documented pre-existing `propertyListFromData:`/GBStorage deprecation floor, zero new warnings). **No regressions** across the full suite: CAP PASS, SAFETY PASS, ACTIVEGATE PASS, PROTO 12/12, CRYPTO ALL PASS, PCAP_IMPORT 3 nets/7 clients, BLE PASS, USBINV usb=10/hid=30/serial=3, LAN 59 services, IFACE 33, REPORT OVERALL PASS; app launched + terminated clean, no instances left. **Deferred (remaining S6.x → these are S5.x):** the **encryption UI** (a passphrase-entry sheet on save/open that calls `+setSessionPassphrase:`, the privacy-prefs toggle for `projectEncryptionEnabled`, the File→Export-report menu action) is **report/export UI = S5.x** — this slice provides the crypto + storage wiring + the memory-only passphrase API the UI will call. Legacy plist `propertyListFromData:`/`dataFromPropertyList:` modernization + GBStorage secure-coding remain the documented S6.x floor.

- **S7.x — Authorized lab features** (Milestone 10): Pineapple-style workflows. *Impl:* pineapple-lab-features-agent · *Rev:* security-abuse-reviewer + capture-safety-reviewer · *Val:* authorized-lab-validation-agent. **[blocked]** — hard dependency: S4.1 + S1.3 + S1.1 must all land first.
- **S8.x — Signing/notarization/CI** (Milestone 14): Developer ID, hardened runtime, notarization, DMG, CI, submodule patch validation. *Impl:* release-engineering-agent · *Rev:* release-reviewer · *Val:* xcode-build-validator + notarization-validation-agent. **Split into S8.1 (cert-independent scaffolding) + S8.2 (the actual signed/notarized run, BLOCKED on the Developer ID cert).**

### S8.1 — Release scaffolding (cert-independent parts of Milestone 14) — ✅ DONE
- **Milestone:** 14 · **Impl:** release-engineering-agent · **Rev:** release-reviewer · **Val:** xcode-build-validator · **Deps:** S0.5 (green Release build); informs/uses S1.2/S3.x permission strings
- **Scope:** Stand up the **entire** Developer ID signing → hardened runtime → notarization → DMG → CI pipeline so it is correct and ready **without** the signing certificate (team `DMP42GVPJ3`, ENV-1, not on this machine). No real `codesign`/`notarytool` calls — those are S8.2.
- **Acceptance:** Clean Release build (signing disabled) still green with the hardened-runtime build-setting changes; scripts shellcheck-clean and degrade gracefully (helpful message + non-zero exit, nothing destructive) without credentials; `.secrets/` gitignored with a placeholder-only `.example`; no secret committed; self-tests not regressed.
- **Outcome:** **(1) Hardened-runtime entitlements:** new `KisMac2.entitlements` (repo root) — **NOT sandboxed** (no `com.apple.security.app-sandbox`; KisMac needs raw BPF/IOKit/CoreWLAN/CoreBluetooth/Apple80211), minimal hardened-runtime keys only: `personal-information.location` (S1.2), `device.bluetooth` (S3.1), `device.usb` (S3.2), each justified inline. **Library validation LEFT ENABLED** — `disable-library-validation` is commented out with a documented justification: BIGL/BIGeneric/GBStorage are signed with the **same** team by the export script, so it isn't needed. Wired into the **Release** config only: `CODE_SIGN_ENTITLEMENTS = KisMac2.entitlements` + `ENABLE_HARDENED_RUNTIME = YES` (both **inert** under `CODE_SIGNING_ALLOWED=NO`, so the unsigned verification build stays green; Debug config untouched). **(2) `scripts/` pipeline** (POSIX bash, `set -euo pipefail`, **shellcheck-clean**, all inputs via env/flags, no secret ever echoed): `lib.sh` (shared paths/env-loader/preflight — prints only var NAMES + present/MISSING), `build_release.sh` (clean Release `xcodebuild archive`, `ENABLE_HARDENED_RUNTIME=YES`, `--unsigned` mode for CI smoke), `sign_export.sh` (generates a `developer-id` exportOptions plist, `-exportArchive`, verifies signature + hardened-runtime flag), `notarize.sh` (`xcrun notarytool submit --wait` with the ASC API key from `.secrets/`, then `stapler staple`+`validate`; zips a `.app` for submission, staples the original), `make_dmg.sh` (modern `hdiutil create -format UDZO` + the existing `DMG Artwork/dmgBackground.tiff` + `/Applications` symlink, optional DMG codesign — **replaces** the legacy `Kismac.dmg` aggregate target that used the removed `hdiutil internet-enable` + a fragile Finder AppleScript), and `release.sh` (orchestrates build→sign→notarize→staple→dmg→notarize-dmg with a `--preflight` mode). **Every script preflights and EXITS non-zero with exact remediation steps when the cert/keys are absent — verified: all of build/sign/notarize/release exit 1 doing nothing destructive; `make_dmg.sh` proven to produce a `hdiutil verify`-valid DMG from the unsigned Release product (cert-independent packaging path).** **(3) CI:** new `.github/workflows/build.yml` (GitHub Actions, `macos-15`) — submodule-recursive checkout, shellcheck, **signing-disabled clean Debug build** asserting `BUILD SUCCEEDED`, then a **headless `KISMAC_*_SELFTEST` smoke** (cap/proto/crypto/pcap-import/safety/active-gate/usb-inventory/iface-inventory) that fails on any logged failure. **Unsigned + secret-free on purpose** (runs on forks); comments mark exactly where GitHub secrets would plug into a separate protected release workflow. Legacy `.travis.yml` (Xcode 8.3 / macOS 10.12) **deprecated** → breadcrumb pointing at the Actions workflow + scripts. **(4) Docs:** new `docs/release.md` — pipeline overview, hardened-runtime/entitlements rationale, install + permission mapping (Location/Bluetooth/Local-Network/pcap), the `.secrets/` setup, the S8.2 "what you must provide" list, troubleshooting, and the **known limitation that live pcap/BPF capture needs root/a privileged helper and will NOT work under a plain hardened-runtime Developer-ID app** (ties the parity 🔍 monitor/channel/handshake rows). **Secrets hygiene:** `.secrets/` gitignored (`*` except `appstoreconnect.env.example`), `dist/` gitignored; the `.example` contains **placeholder names only** (`DEVELOPMENT_TEAM=`/`CODESIGN_IDENTITY=`/`ASC_API_KEY_ID=`/`ASC_API_ISSUER_ID=`/`ASC_API_KEY_PATH=`); `git check-ignore` confirms real env + `.p8` are ignored and the `.example` is tracked. **Verify:** clean **Release** build (signing disabled) with the new settings **BUILD SUCCEEDED** (Xcode 26.5 / macOS 27 / arm64); `shellcheck -x scripts/*.sh` clean; **no regressions** — clean Debug build green + `KISMAC_CAP_SELFTEST` + `KISMAC_PROTO_SELFTEST` PASS. **No real `codesign`/`notarytool` executed** (none possible without the cert).

### S8.2 — Signed + notarized release run — ⛔ BLOCKED (Developer ID cert / ASC key, ENV-1)
- **Milestone:** 14 · **Impl:** release-engineering-agent · **Rev:** release-reviewer · **Val:** notarization-validation-agent · **Deps:** S8.1
- **Scope:** Run the S8.1 pipeline for real: `scripts/release.sh` → Developer ID signed, hardened-runtime, **notarized + stapled** `KisMac2.app` and `KisMac2.dmg`; confirm Gatekeeper acceptance on a clean machine; wire the protected CI release path (GitHub secrets).
- **Blocked on (the user must provide on the signing machine):** (1) a **Developer ID Application** certificate + private key for team `DMP42GVPJ3` in the login Keychain; (2) `.secrets/appstoreconnect.env` filled from the `.example` (`DEVELOPMENT_TEAM`, `CODESIGN_IDENTITY`, `ASC_API_KEY_ID`, `ASC_API_ISSUER_ID`, `ASC_API_KEY_PATH`); (3) the ASC API key at `.secrets/private_keys/AuthKey_<KEY_ID>.p8`. See `docs/release.md` § "What you must provide". Also tracked: a future signed/notarized **privileged BPF helper** for live capture (the documented hardened-runtime limitation).

---

## Dependency summary

```
S0.1 → S0.6 → S0.2,S0.3,S0.4 → S0.5 ─┬─→ S1.1 ─┬─→ S1.3 ─┬─→ S1.4
       (re-run S0.1 after S0.6+S0.2)  │         │         │
                              │         │         ├─→ S2.1, S2.2, S2.4
                              ├─→ S1.2  │         ├─→ S3.1..S3.4
                              │         │         └─→ S4.1 ─→ S4.2 ─→ S7.x
                              └─────────┴─→ S2.3        S4.1 ─→ S6.x
                                                        S1.3 ─→ S5.x ─→ S8.x
```

Critical path: **S0.x (compile baseline) → S1.1 (probe) → S1.3 (capability engine)**.
Almost everything depends on the capability engine; the probe is what retires the
🔍 `needs-probe` rows. Start Wave 0 now.

## PR gate (every slice)

Per `docs/subagent-orchestration.md`: one implementation owner, one independent
reviewer, one validation owner, **plus** build evidence (`xcode-build-validator`) if
code changed, capability-matrix update if hardware/API behavior changed, parity-matrix
update (`docs/original-feature-parity.md`) if original behavior changed, `security-abuse-reviewer`
if active cyber functionality changed, `privacy-reviewer` if identifiers/locations/
captures/reports/exports changed.

## Final review fixes

Defects found by the final holistic code review of `setup/subagent-orchestration`
and resolved (all on this branch; Debug + Release builds green, full self-test
suite green incl. extended `KISMAC_ACTIVEGATE_SELFTEST`):

- **C1 (CRITICAL safety)** — `-beaconFlood` and `-authFloodNetwork:` in
  `Sources/Core/WaveScanner.mm` bypassed the S4.2 active-op gate. Both now route
  through `-[ScanController allowActiveOperation:feature:target:targetIsClient:]`
  before transmitting (auth-flood: targeted at the AP BSSID; beacon-flood:
  broadcast/nil target, fails closed exactly like `-setDeauthingAll:`). They can
  no longer transmit without engine-available + in-scope campaign + audit.
  Proved by new assertions 4i/4j/4k in `KMActiveGateSelfTest`.
- **C2 (CRITICAL memory-safety)** — `Sources/Crypto/WaveNetWPACrack.m`: `digest`
  was `UInt8[16]` but `fast_hmac_sha1()` writes 20 bytes (4-byte stack overflow).
  Resized to `UInt8 digest[20]`; downstream MIC compare still reads 16.
- **C3** — `KisMac2.entitlements`: removed the inert App-Sandbox keys
  (`personal-information.location`, `device.bluetooth`, `device.usb`) on this
  non-sandboxed app. Dict is now empty; comment explains Location/Bluetooth/LAN
  are handled via TCC + the `NS*UsageDescription` Info.plist strings, and that an
  empty entitlements file is correct for the hardened-runtime Release build.
- **H1 (HIGH)** — `Sources/Core/WavePacket.mm` `-parseTaggedData:`: the legacy
  TLV loop could over-read up to ~3 bytes past a truncated IE from an
  attacker-controlled imported pcap. Added a check-before-dispatch bounds guard
  (`if (elementLen + 2 > length) break;`) and tightened the RSN case
  (`len < 6 || length < len + 2`). Well-formed frames unchanged.
- **H2 (HIGH)** — `Sources/Crypto/WaveNetLEAPCrack.m`: the wordlist reader
  unconditionally truncated the last char, dropping a real password char on
  newline-less lines (last line / >=89-char line split at the fgets boundary).
  Now strips only actual trailing CR/LF (mirrors `WaveNetWPACrack.m`), keeping
  the empty-line underflow guard.
- **H3 (HIGH)** — `Sources/Safety/KMCampaignManager.m` `-recordActiveOperation:`:
  added an explicit belt-and-suspenders emergency-stop latch at the op-site gate
  (matching `-engageEmergencyStop`). Refuses + audits under e-stop. Behavior is
  unchanged today (`hasActiveScope` already honors e-stop); hardens against
  future refactors.
